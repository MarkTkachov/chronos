import styled from "styled-components";

export const ParticipantsList = styled.div`
    display: flex;
    max-height: 100px;
    flex-direction: column;
    gap: 5px;
    background: inherit;
    align-items: center;
    overflow-y: auto;
`;
