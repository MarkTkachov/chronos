import styled from "styled-components";

export const Row = styled.div`
    display: flex;
    flex-direction: row;
    flex-wrap: wrap;
    place-items: center;
    justify-content: space-around;
    gap: 10px;
`;
